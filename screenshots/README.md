# Screenshots

`comparison.png` is generated by the offline simulation (`make sim`).

After running the live demo (`make demo`), capture and add here:

- `replicas-ahead-of-ramp.png` — `kubectl get deploy demo-app -w` showing the
  replica count rising *before* load peaks (the forecast working).
- `grafana-rps-vs-replicas.png` — Grafana panel with demo-app RPS and replica
  count overlaid across a full ramp.
- `reactive-catches-spike.png` — the moment the surprise spike fires and the
  reactive KEDA trigger scales up.

Reference them in the root README under the live-demo section.
